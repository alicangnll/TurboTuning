--- common/arg.cpp
+++ common/arg.cpp
@@ -2025,6 +2025,7 @@
         [](common_params & params, const std::string & value) {
             params.cache_type_k = kv_cache_type_from_str(value);
             if (params.cache_type_k == GGML_TYPE_TURBO2_0 || params.cache_type_k == GGML_TYPE_TURBO3_0 || params.cache_type_k == GGML_TYPE_TURBO4_0) {
+                fprintf(stderr, "DEBUG: Auto-enabling turbo_async for cache_type_k\n");
                 params.turbo_async = true;
             }
         }
@@ -2037,6 +2038,7 @@
         [](common_params & params, const std::string & value) {
             params.cache_type_v = kv_cache_type_from_str(value);
             if (params.cache_type_v == GGML_TYPE_TURBO2_0 || params.cache_type_v == GGML_TYPE_TURBO3_0 || params.cache_type_v == GGML_TYPE_TURBO4_0) {
+                fprintf(stderr, "DEBUG: Auto-enabling turbo_async for cache_type_v\n");
                 params.turbo_async = true;
             }
         }
